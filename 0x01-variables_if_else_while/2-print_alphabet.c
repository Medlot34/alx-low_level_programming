#include <stdio.h>
/**
*main -Entry point
*Discription : 'check for number'
*Return: always 0
*/
int main(void)
{
	int n = 95;

	while (n <= 120)
	{
		putchar (n);
		n++;
	}
	putchar('\n');
	return (0);
}
